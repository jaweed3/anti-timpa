version: '3.8'

services:
  api:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./.env
    environment:
      - NARA_ROUTER_API=${NARA_ROUTER_API:-}
    restart: unless-stopped
